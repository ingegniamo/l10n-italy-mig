<odoo>

    <record id="purchase_order_form_view_delivery_note" model="ir.ui.view">
        <field name="name">purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase_stock.purchase_order_view_form_inherit" />
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <field name="delivery_note_ids" invisible="1" />
                <button type="object" class="oe_stat_button" name="goto_delivery_notes" icon="fa-pencil-square-o" invisible="delivery_note_count == 0">
                    <field name="delivery_note_count" widget="statinfo" string="Delivery Notes" />
                </button>
            </xpath>
       </field>
    </record>

</odoo>